generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model FormEntry {
  id           Int      @id @default(autoincrement())
  model        String
  licenseLevel Int
  quantity     Int
}
